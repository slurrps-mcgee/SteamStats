{
  "name": "steamstats",
  "version": "0.3.1",
  "private": true,
  "description": "SteamStats monorepo - Angular frontend and Fastify backend.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "generate:api": "npm run dump:openapi --workspace=backend && npm run generate:api --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run start --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format:check": "npm run format:check --workspace=backend && npm run format:check --workspace=frontend"
  },
  "dependencies": {
  },
  "overrides": {
    "typescript": "~6.0.3"
  }
}
