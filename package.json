{
  "name": "steamstats",
  "version": "0.3.1",
  "private": true,
  "description": "SteamStats monorepo - Angular frontend and Fastify backend.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "generate:api": "npm run dump:openapi --workspace=backend && npm run generate:api --workspace=frontend",
    "format:all": "npm run format --workspace=backend && npm run format --workspace=frontend",
    "check:format": "npm run format:check --workspace=backend && npm run format:check --workspace=frontend",
    "check:openapi": "npm run generate:api && git diff --exit-code -- frontend/src/app/api/openapi.json frontend/src/app/api/generated/",
    "check:types": "npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend",
    "check:audit": "npm audit --audit-level=high --workspace=backend && npm audit --audit-level=high --workspace=frontend",
    "check:build": "npm run build --workspace=backend && npm run build --workspace=frontend -- --configuration production",
    "check:test": "npm run test --workspace=backend",
    "check:all": "npm run check:audit && npm run check:format && npm run check:types && npm run check:openapi && npm run check:build && npm run check:test",
    "test:all": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run start --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "docker:compose:clear": "docker compose down -v && docker compose build && docker compose up",
    "docker:compose:up": "docker compose build && docker compose up",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format:check": "npm run format:check --workspace=backend && npm run format:check --workspace=frontend"
  },
  "dependencies": {
  },
  "overrides": {
    "typescript": "~6.0.3"
  }
}
