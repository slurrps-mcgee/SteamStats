{
  "name": "@steamstats/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types used by the SteamStats frontend and backend.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
